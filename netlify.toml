# Netlify configuration for UniShare React SPA

[build]
  command = "cd frontend && npm ci && npm run build"
  publish = "frontend/build"

# Redirect all non-file requests to index.html for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
